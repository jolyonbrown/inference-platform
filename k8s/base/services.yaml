# Placeholder services
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: inference-platform
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: inference-platform
spec:
  selector:
    app: vllm
  ports:
    - name: http
      port: 80
      targetPort: 8000
