# Placeholder vLLM deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm
  namespace: inference-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm
  template:
    metadata:
      labels:
        app: vllm
    spec:
      containers:
        - name: vllm
          image: vllm:latest
